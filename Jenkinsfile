pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'GitHub project connected successfully'
            }
        }

        stage('Build') {
            steps {
                echo 'Build successful'
            }
        }
    }
}